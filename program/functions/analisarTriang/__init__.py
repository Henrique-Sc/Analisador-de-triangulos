from ..format import *
from math import sqrt, degrees
from numpy import arccos


def analise(dados):
    for i, t in enumerate(dados):
        title(f'{i + 1}º triângulo', cor=3)
        sleep(1)


        # de um triangulo pro outro
        sleep(1.5)